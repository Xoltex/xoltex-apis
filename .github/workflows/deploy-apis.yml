name: Deploy Apis

on:
  push:
    paths:
      - '*/**'
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Debug paso inicial
        run: echo "✅ Workflow disparado correctamente"